# ── 데이터베이스 ────────────────────────────────
# 개발: SQLite (기본값, 변경 불필요)
# DATABASE_URL=sqlite:///./iam_anomaly.db
# 운영: PostgreSQL로 전환
# DATABASE_URL=postgresql://user:password@localhost:5432/iam_anomaly

# ── API 서버 ────────────────────────────────────
# API_HOST=0.0.0.0
# API_PORT=8000

# ── 대시보드 ────────────────────────────────────
# API_BASE_URL=http://localhost:8000

# ── LLM (Phase 4 - 리포트 자동생성용) ──────────
# ANTHROPIC_API_KEY=sk-ant-...
